<h1>編輯課程主題 - <%= @category.name %></h1>

<%= form_for [:admin, @category], :method => :put do |f| %>

  <%= render 'form', f: f %>

  <%= f.submit 'Save' %>
  <%= link_to 'Cancel', admin_programs_path %>

<% end %>
