<h1>編輯課程類別 - <%= @type.name %></h1>

<%= form_for [:admin, @type], :method => :put do |f| %>

  <%= render 'form', f: f %>

  <%= f.submit 'Save' %>
  <%= link_to 'Cancel', admin_programs_path %>

<% end %>
